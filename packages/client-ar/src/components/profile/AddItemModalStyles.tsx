import styled from "styled-components";
import {Form} from "formik";

export const AddItemModalStyles = styled(Form)`
  display: flex;
  flex-direction: column;
  justify-content: center;
  width: 100%
`;
